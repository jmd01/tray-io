import React from 'react'
import {render} from '@testing-library/react'

import ConnectorList from './index'

describe('<ConnectorList />', () => {

    it('Renders the list', () => {
        // Expect getByTestId('connectorList')).toBeInTheDocument()
    })

    it('Renders list items',() => {
        // Mock an array of list items and pass to props
        // lis = getAllBy('li')
        // Expect (lis.length).toEqual()
        // Expect mock[0].name .toBeInTheDocument()
    })

})